import ChatLayout from '../layout/ChatLayout';
import ChatInterface from '../chat/ChatInterface';

const SchemesPage = () => {
  return (
    <ChatLayout domain="schemes">
      <ChatInterface domain="schemes" />
    </ChatLayout>
  );
};

export default SchemesPage;